{
  "name": "sasanoha.dev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check && npm test",
    "test": "node --test test/*.test.ts",
    "build": "npm run check && astro build",
    "preview": "astro preview",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run",
    "api:dev": "wrangler dev --config wrangler.api.jsonc",
    "api:d1:migrate:local": "wrangler d1 migrations apply sasanoha-dev-cms --local --config wrangler.api.jsonc",
    "api:d1:migrate:remote": "node scripts/verify-api-provisioning.mjs d1 && wrangler d1 migrations apply sasanoha-dev-cms --remote --config wrangler.api.jsonc",
    "api:deploy:dry-run": "wrangler deploy --dry-run --config wrangler.api.jsonc",
    "api:deploy": "node scripts/verify-api-provisioning.mjs deploy && wrangler deploy --config wrangler.api.jsonc"
  },
  "dependencies": {
    "@astrojs/vue": "^7.0.1",
    "astro": "^7.0.6",
    "sass": "^1.101.0",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^24.13.5",
    "typescript": "^6.0.3",
    "wrangler": "^4.130.0"
  }
}
